version: "3"
services:
  web:
    image: node:14
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: sh -c "node Server/server.js"

    depends_on:
      - mongodb
  mongodb:
    image: mongo
    ports:
      - "27018:27017"
